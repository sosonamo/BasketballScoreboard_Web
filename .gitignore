# Dependency directories (의존성 패키지 디렉토리)
node_modules/
jspm_packages/

# Build outputs (빌드 결과물)
dist/
build/

# Logs (로그 파일)
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables (환경 변수 및 비밀번호 파일 - 보안 필수!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS generated files (운영체제 생성 파일)
.DS_Store
Thumbs.db

# 개인 설정
Backup/
Source/
*.txt